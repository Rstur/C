/*
请编写一个函数fun，它的功能是：将一个字符串转换为一个整数(不得调用C语言提供的将字符串转换为整数的函数)。
*/

#include <stdio.h>
#include <string.h>

void fun(char str[100])
{
}

int main(void)
{
    char str[100];
    printf("请输入一个字符串:\n");
    scanf("%s", str);
    fun(str);
}